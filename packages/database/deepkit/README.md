# database-deepkit

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build database-deepkit` to build the library.

## Running unit tests

Run `nx test database-deepkit` to execute the unit tests via [Jest](https://jestjs.io).
